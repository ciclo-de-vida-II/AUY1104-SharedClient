apiVersion: apps/v1
kind: Deployment
metadata:
  name: __APP_NAME__-stable
  labels:
    app: __APP_NAME__
    track: stable
spec:
  replicas: __STABLE_REPLICAS__
  selector:
    matchLabels:
      app: __APP_NAME__
      track: stable
  template:
    metadata:
      labels:
        app: __APP_NAME__
        track: stable
    spec:
      containers:
        - name: api
          image: __IMAGE__:__STABLE_TAG__
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
